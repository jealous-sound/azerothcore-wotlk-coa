# Withheld creature candidates

These candidates require separate metadata or behavior work; none is inserted by this migration.

## 26 — Cabal Ritualist

Candidate donor: 18794.

context_action: Action is outside the conservative portable-action allowlist

## 42850 — Irespeaker

Candidate donor: 24999.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 61493 — Auchenai Soulpriest

Candidate donor: 18493.

context_action: Action is outside the conservative portable-action allowlist

## 61495 — Auchenai Vindicator

Candidate donor: 18495.

context_action: Action is outside the conservative portable-action allowlist

## 61497 — Auchenai Monk

Candidate donor: 18497.

context_action: Action is outside the conservative portable-action allowlist

## 61712 — Cabal Ritualist

Candidate donor: 18794.

context_action: Action is outside the conservative portable-action allowlist

## 61715 — Auchenai Soulpriest

Candidate donor: 18493.

context_action: Action is outside the conservative portable-action allowlist

## 61716 — Auchenai Vindicator

Candidate donor: 18495.

context_action: Action is outside the conservative portable-action allowlist

## 61717 — Auchenai Monk

Candidate donor: 18497.

context_action: Action is outside the conservative portable-action allowlist

## 73401 — Crimson Elite

Candidate donor: 12128.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 73484 — Amani'shi Guardian

Candidate donor: 23597.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 80262 — Skul

Candidate donor: 10393.

context_action: Action is outside the conservative portable-action allowlist

## 80266 — Vectus

Candidate donor: 10432.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 80274 — Ramstein the Gorger

Candidate donor: 10439.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 80278 — The Ravenian

Candidate donor: 10507.

context_action: Action is outside the conservative portable-action allowlist

## 80282 — Timmy the Cruel

Candidate donor: 10808.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 80284 — Stonespine

Candidate donor: 10809.

context_action: Action is outside the conservative portable-action allowlist

## 80288 — Lorekeeper Polkelt

Candidate donor: 10901.

context_action: Action is outside the conservative portable-action allowlist

## 80292 — Zevrim Thornhoof

Candidate donor: 11490.

context_action: Action is outside the conservative portable-action allowlist

## 80294 — Alzzin the Wildshaper

Candidate donor: 11492.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 80296 — Immol'thar

Candidate donor: 11496.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 80324 — Blazing Trickster

Candidate donor: 20905.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 80983 — Oblivion Mage

Candidate donor: 25597.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 82922 — Murloc Oracle

Candidate donor: 517.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 82928 — Murloc Tidecaller

Candidate donor: 545.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 82930 — Murloc Minor Tidecaller

Candidate donor: 548.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 82988 — Riverpaw Shaman

Candidate donor: 1065.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 83056 — Highlord Taelan Fordring

Candidate donor: 1842.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 83081 — Pyrewood Sentry

Candidate donor: 1894.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 83082 — Pyrewood Elder

Candidate donor: 1895.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 83537 — Suffering Victim

Candidate donor: 6547.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

## 83854 — Malyfous Darkhammer

Candidate donor: 10637.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 83984 — Nathanos Blightcaller

Candidate donor: 11878.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

## 84010 — Demetria

Candidate donor: 12339.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

implicit_absolute_position: Core converts this self target to an absolute world position

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 84512 — Urga'zz

Candidate donor: 18976.

context_action: Action is outside the conservative portable-action allowlist

## 84583 — Arzeth the Merciless

Candidate donor: 19354.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 84709 — Felblood Initiate

Candidate donor: 24918.

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 92381 — Volatile Fiend

Candidate donor: 25851.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

## 98765 — Aku'mai Snapjaw

Candidate donor: 4825.

context_action: Action is outside the conservative portable-action allowlist

## 100377 — Flesh Hunter

Candidate donor: 15335.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

## 114831 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114832 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114833 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114835 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114836 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114837 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114838 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114839 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114840 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 114841 — Sacrificed Troll

Candidate donor: 14826.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 124683 — Sunblade Mage Guard

Candidate donor: 24683.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124684 — Sunblade Blood Knight

Candidate donor: 24684.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124685 — Sunblade Magister

Candidate donor: 24685.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124686 — Sunblade Warlock

Candidate donor: 24686.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124687 — Sunblade Physician

Candidate donor: 24687.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124688 — Wretched Skulker

Candidate donor: 24688.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124689 — Wretched Bruiser

Candidate donor: 24689.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 124690 — Wretched Husk

Candidate donor: 24690.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 128860 — Sartharion - Heroic

Candidate donor: 31311.

invalid display probability weights

## 130449 — Vesperon - Heroic

Candidate donor: 31535.

invalid display probability weights

## 130451 — Shadron - Heroic

Candidate donor: 31520.

invalid display probability weights

## 130452 — Tenebron - Heroic

Candidate donor: 31534.

invalid display probability weights

## 178190 — Durnholde Sentry

Candidate donor: 17819.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 178330 — Durnholde Warden

Candidate donor: 17833.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 178600 — Durnholde Veteran

Candidate donor: 17860.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 178951 — Skeletal Reaver

Candidate donor: 32467.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 224683 — Sunblade Mage Guard

Candidate donor: 24683.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224684 — Sunblade Blood Knight

Candidate donor: 24684.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224685 — Sunblade Magister

Candidate donor: 24685.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224686 — Sunblade Warlock

Candidate donor: 24686.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224687 — Sunblade Physician

Candidate donor: 24687.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224688 — Wretched Skulker

Candidate donor: 24688.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224689 — Wretched Bruiser

Candidate donor: 24689.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 224690 — Wretched Husk

Candidate donor: 24690.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 276940 — Haunting Spirit

Candidate donor: 4958.

context_action: Action is outside the conservative portable-action allowlist

## 300000 — Fel Orc Convert

Candidate donor: 17083.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 300044 — Guard Mol'dar

Candidate donor: 14326.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

## 300078 — Risen Spirit

Candidate donor: 23554.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 416164 — Shade of Naxxramas

Candidate donor: 16164.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 428501 — Vicious Geist

Candidate donor: 31147.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 428502 — Irespeaker

Candidate donor: 24999.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 428523 — Scarlet Defender

Candidate donor: 4298.

context_action: Action is outside the conservative portable-action allowlist

## 428534 — Onslaught Knight

Candidate donor: 27206.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 428580 — Interrogator Vishas

Candidate donor: 3983.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

## 428601 — Vicious Geist

Candidate donor: 31147.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 475282 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 475283 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 475284 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 475285 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 475286 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 475287 — Fel Warden

Candidate donor: 22273.

context_action: Action is outside the conservative portable-action allowlist

## 550341 — Blackwind Warp Chaser

Candidate donor: 23219.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 550820 — Baelmon the Hound-Master

Candidate donor: 19747.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

external_talker: Dialogue target may make another creature use the donor text group

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 550857 — Bloodmaul Dire Wolf

Candidate donor: 20058.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 550859 — Grimnok Battleborn

Candidate donor: 20095.

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 550874 — Wrath Hound

Candidate donor: 20557.

context_action: Action is outside the conservative portable-action allowlist

## 550898 — Bladespire Chef

Candidate donor: 20756.

external_talker: Dialogue target may make another creature use the donor text group

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 550902 — Gnosh Brognat

Candidate donor: 20768.

external_talker: Dialogue target may make another creature use the donor text group

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 550972 — Raven's Wood Leafbeard

Candidate donor: 21326.

context_action: Action is outside the conservative portable-action allowlist

## 550979 — Wyrmcult Blackwhelp

Candidate donor: 21387.

context_action: Action is outside the conservative portable-action allowlist

## 551069 — Wrath Speaker

Candidate donor: 22195.

context_action: Action is outside the conservative portable-action allowlist

## 551071 — Fear Fiend

Candidate donor: 22204.

context_action: Action is outside the conservative portable-action allowlist

## 551090 — Furnace Guard

Candidate donor: 22291.

context_action: Action is outside the conservative portable-action allowlist

## 551318 — Battle-Mage Dathric

Candidate donor: 19543.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 551319 — Conjurer Luminrath

Candidate donor: 19544.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 551320 — Cohlien Frostweaver

Candidate donor: 19545.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 551321 — Abjurist Belmara

Candidate donor: 19546.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 551483 — Tormented Soul

Candidate donor: 20512.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 552017 — Dragonmaw Transporter

Candidate donor: 23188.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 552031 — Disobedient Dragonmaw Peon

Candidate donor: 23311.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

## 552198 — Engineer "Spark" Overgrind

Candidate donor: 17243.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 552421 — Matis the Cruel

Candidate donor: 17664.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 700032 — Scarlet Initiate

Candidate donor: 1507.

external_talker: Dialogue target may make another creature use the donor text group

## 700039 — Scarlet Initiate

Candidate donor: 1507.

external_talker: Dialogue target may make another creature use the donor text group

## 700700 — Horde Guard

Candidate donor: 3501.

no_template_smart_scripts: Donor 3501 has no positive template-level SmartAI rows

## 700707 — Nazgrel

Candidate donor: 3230.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 700777 — Warsong Captain

Candidate donor: 25446.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 720002 — Skeleton

Candidate donor: 6412.

context_action: Action is outside the conservative portable-action allowlist

## 750054 — The Windreaver

Candidate donor: 14454.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 750057 — Watery Invader

Candidate donor: 14458.

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 750058 — Princess Tempestria

Candidate donor: 14457.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 750059 — Blazing Invader

Candidate donor: 14460.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 750060 — Baron Charr

Candidate donor: 14461.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 750061 — Thundering Invader

Candidate donor: 14462.

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 750062 — Avalanchion

Candidate donor: 14464.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 775128 — Murloc Minor Tidecaller

Candidate donor: 548.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 853410 — Horde Defender

Candidate donor: 9457.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 854410 — Horde Defender

Candidate donor: 9457.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 855410 — Horde Defender

Candidate donor: 9457.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 855603 — Horde Defender

Candidate donor: 9457.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1011830 — Hakkari Priest

Candidate donor: 11830.

external_talker: Dialogue target may make another creature use the donor text group

## 1012557 — Grethok the Controller

Candidate donor: 12557.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1015300 — Vekniss Drone

Candidate donor: 15300.

context_action: Action is outside the conservative portable-action allowlist

## 1015324 — Qiraji Gladiator

Candidate donor: 15324.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1015335 — Flesh Hunter

Candidate donor: 15335.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

## 1015502 — Andorgos

Candidate donor: 15502.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

## 1015521 — Hive'Zara Hatchling

Candidate donor: 15521.

context_action: Action is outside the conservative portable-action allowlist

## 1025597 — Oblivion Mage

Candidate donor: 25597.

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 1082922 — Murloc Oracle

Candidate donor: 517.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1082928 — Murloc Tidecaller

Candidate donor: 545.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1082930 — Murloc Minor Tidecaller

Candidate donor: 548.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1082988 — Riverpaw Shaman

Candidate donor: 1065.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1083081 — Pyrewood Sentry

Candidate donor: 1894.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 1083082 — Pyrewood Elder

Candidate donor: 1895.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

context_condition: Condition depends on unreviewed quest, spawn, reference, or world context

## 1083854 — Malyfous Darkhammer

Candidate donor: 10637.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 1083984 — Nathanos Blightcaller

Candidate donor: 11878.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

## 1084010 — Demetria

Candidate donor: 12339.

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

implicit_absolute_position: Core converts this self target to an absolute world position

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 3025022 — Valhalas Vargul

Candidate donor: 30250.

context_action: Action is outside the conservative portable-action allowlist

## 4004825 — Aku'mai Snapjaw

Candidate donor: 4825.

context_action: Action is outside the conservative portable-action allowlist

## 4006243 — Gelihast

Candidate donor: 6243.

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 5050506 — Lord Alexei Barov

Candidate donor: 10504.

context_action: Action is outside the conservative portable-action allowlist

## 5527748 — Mmmrrrggglll

Candidate donor: 15937.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 5528121 — Dawnblade Marksman

Candidate donor: 24979.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 5528125 — Irespeaker

Candidate donor: 24999.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 5528127 — Unleashed Hellion

Candidate donor: 25002.

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

## 5528151 — Greengill Slave

Candidate donor: 25084.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 5528278 — Matis the Cruel

Candidate donor: 17664.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 5528503 — Matis the Cruel

Candidate donor: 17664.

context_action: Action is outside the conservative portable-action allowlist

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist

## 5528697 — Engineer "Spark" Overgrind

Candidate donor: 17243.

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_event: Event depends on quest, gossip, path, transport, instance, GUID, or world context

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

external_talker: Dialogue target may make another creature use the donor text group

context_action: Action is outside the conservative portable-action allowlist

context_target: Target uses an entry, GUID, position, gameobject, instance, or formation selector

context_action: Action is outside the conservative portable-action allowlist
